export class Book {
  constructor({id, author, title, price, description}) {
    this.id = id;
    this.author = author;
    this.title = title;
    this.price = price;
    this.description = description;
  }
}